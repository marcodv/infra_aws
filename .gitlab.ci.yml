stages:
  - build-docker
#  - test
#  - production

build terraform image:
  stage: build-docker
  image: docker:latest
  services:
    - docker:dind
  script:
    - docker build -t hello .



#deploy to test:
#  stage: test
#  environment: test

#deploy to prod:
#  stage: prod
#  environment: prod